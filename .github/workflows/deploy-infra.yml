name: Deploy Infrastructure

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch name to deploy from'
        required: true
        type: string
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - dev
          - prod

permissions:
  id-token: write
  contents: read

jobs:
  deploy-infra:
    name: Deploy Bicep Infrastructure
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}

      - name: Login to Azure with OIDC federated credentials
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Set deployment variables
        id: vars
        run: |
          DEPLOYMENT_NAME="eklee-keyvault-$(date +%Y%m%d-%H%M%S)"
          echo "environment=${{ inputs.environment }}" >> "$GITHUB_OUTPUT"
          echo "deploymentName=$DEPLOYMENT_NAME" >> "$GITHUB_OUTPUT"

      - name: Deploy Bicep template
        id: deploy
        run: |
          az deployment group create \
            --resource-group "${{ vars.RESOURCE_GROUP }}" \
            --template-file Deployment/main.bicep \
            --name "${{ steps.vars.outputs.deploymentName }}" \
            --parameters \
              environment=${{ steps.vars.outputs.environment }} \
              containerRegistryName=${{ vars.ACR_NAME }} \
              containerRegistryResourceGroup=${{ vars.ACR_RESOURCE_GROUP }}
