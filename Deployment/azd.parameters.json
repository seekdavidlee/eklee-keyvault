{
  "parameters": {
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "clientId": {
      "value": "${APP_CLIENT_ID}"
    },
    "containerImage": {
      "value": "${CONTAINER_IMAGE}"
    },
    "customDomainName": {
      "value": "${CUSTOM_DOMAIN_NAME}"
    }
  }
}
